type Diary = 
 record {
   content: text;
   createdAt: text;
   diaryLabel: text;
   id: nat;
 };
service : {
  createDiary: (nat, text, text, text) -> () oneway;
  readDiaries: () -> (vec Diary) query;
  removeDiaries: (nat) -> () oneway;
}
